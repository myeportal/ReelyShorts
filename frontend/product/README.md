# Protected product folder

Place the final buyer ZIP here as:

- `reelyshorts-white-label.zip`

This folder is blocked from public direct access by `vercel.json`.
The secure download flow serves the ZIP only through `/api/download-product` after verified Stripe payment.
